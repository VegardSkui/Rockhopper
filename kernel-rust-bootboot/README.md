# kernel-rust-bootboot

A minimal kernel example written in Rust and loaded on UEFI using [BOOTBOOT](https://gitlab.com/bztsrc/bootboot).

Rename `bootboot.efi` to `BOOTX64.EFI` and place inside `disk/EFI/BOOT`.
